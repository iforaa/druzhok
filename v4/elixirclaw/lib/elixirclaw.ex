defmodule ElixirClaw do
  @moduledoc """
  ElixirClaw — Standalone AI agent runtime extracted from Druzhok v3's PiCore.

  Provides an HTTP/WebSocket gateway around the PiCore agent loop,
  designed to run in a Docker container managed by the Druzhok v4 orchestrator.
  """
end
